# Companion App (Expo Go)

This is a minimal Expo Go (managed) React Native app scaffolded inside the `companion-app` folder and configured for TypeScript.

Run locally:

```powershell
cd companion-app/companion-app
npm install
npm run start
```

Open the Expo Go app on your device and scan the QR code shown by the dev server.
